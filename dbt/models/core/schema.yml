version: 2

models:
    - name: fact_trips
      description: >
        Trip made by bikes.
        
      columns:
          - name: ride_id
            description: Primary key for this table, generated with a concatenation of vendorid+pickup_datetime
            # tests:
            #     - unique:
            #         severity: warn
            #     - not_null:
            #         severity: warn